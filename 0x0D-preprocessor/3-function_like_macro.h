#ifndef ABS_H
#define ABS_H
/*
*  func-like macro, that computes the absolute value of a number x
* @x: number to convert to absolute value
*/
#define ABS(x) ((x) < 0 ? ((x) * -1) : ((x) * 1))

#endif /* ABS_H */
